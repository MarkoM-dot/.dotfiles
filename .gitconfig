[alias]
    co = checkout
    br = branch
    cm = commit -m
    st = status
    sw = switch

[includeIf "gitdir:~/research/"]
    path = .gitconfig.d/.research.gitconfig

[includeIf "gitdir:~/work/"]
    path = .work.gitconfig
[init]
	defaultBranch = main
